<configuration>
    <!-- Console Appender for general logs -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender for actions -->
    <appender name="ACTIONS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/actions.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/actions.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>10</maxHistory>
            <totalSizeCap>100MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger for actions -->
    <logger name="org.example.ordermanagement.actions" level="INFO" additivity="false">
        <appender-ref ref="ACTIONS_FILE" />
    </logger>

    <!-- Root Logger (general logs to console) -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
